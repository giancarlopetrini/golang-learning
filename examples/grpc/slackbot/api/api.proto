syntax = "proto3";

package api;

service SlackCheck {
    rpc GetStuff(GreetMessage) returns (ResponseMessage) {}
}

message GreetMessage {
    string greeting = 1;
}

message ResponseMessage {
    string response = 1;
}